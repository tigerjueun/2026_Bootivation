# 도율님 파트 추가 체크리스트

- 최종 ROS2 코드·launch·config·pipeline YAML
- YOLO/추적 가중치와 클래스 매핑
- requirements와 실행 명령
- `/retail/customer_state` 정의와 ZMQ transmitter
- 결과 이미지·영상·로그
- 재현 README

대용량 모델은 Git LFS로 등록하고 `vision/*` 브랜치에서 PR로 추가합니다.
